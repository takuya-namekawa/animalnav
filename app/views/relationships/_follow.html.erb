<table class='table'>
   <thead>
     <tr>
      <th>プロフィール画像</th>
      <th>名前</th>
      <th>自己紹介</th>
      <th colspan="3"></th>
    </tr>
</thead>
<tbody>
  <% if users.exists? %>
  <% users.each do |user| %>
    <tr>
      <td><%= image_tag user.get_profile_image(200,200), class:"mt-3 rounded-circle"; %></td>
      <td><%= user.username %></td>
      <td><%= user.profile %></td>
    </tr>
</tbody>
<% end %>

<% else %>
<p>ユーザーはいません</p>
<% end %>
</table>